ABOUT
===
This tool is a command line utility to take HTML file and generate proper PNG screenshot from it
Actively used by `aiva-create` project.

INSTALLATION:
===
- requires imagemagick command line utilifies. `apt-get install imagemagick` for Ubuntu/Debian.
- run `npm install` to install javascript dependencies into node_mosules/ folder
